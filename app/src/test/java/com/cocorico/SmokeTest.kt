package com.cocorico

import org.junit.Assert.assertEquals
import org.junit.Test

class SmokeTest {
    @Test
    fun `la chaine de tests unitaires est operationnelle`() {
        assertEquals(4, 2 + 2)
    }
}
